import { PageLoading } from "@/components/ui/page-loading";

/** Route-segment skeleton (ui-17-loading-coverage): EVERY navigation paints
 *  the page anatomy instantly — no route ships without a placeholder. */
export default function SegmentLoading() {
  return <PageLoading />;
}
