/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Product code arrives in later milestones (M4.6 thin-slice UI, M8 workspace).
  // Keep this config minimal and dependency-free until then.
};

export default nextConfig;
